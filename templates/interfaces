// Author : readyGo "JitenP@Outlook.Com"
// This code is generated by template engine. You are free to make amendments as and where required
package interfaces

import (
database "{{.Root}}/database/mongo"
	"{{.Root}}/models"
)

// Connection interface
type Connection interface {
	GetConnection(url, dbName string) (*database.Database, error)
}
{{ range .Models }}
// {{.Name}} Interface is to define all {{.Name | ToLower}} related methods
type {{.Name}}Interface interface {
    Crate{{.Name}}({{.Name | ToLower}} *models.{{.Name}})(interface{}, error)
	Update{{.Name}}ByID(id string, data map[string]interface{}) (result interface{}, err error)
	Delete{{.Name}}ByID(id string) (result interface{}, err error)
	Get{{.Name}}ByID(id string) (*models.{{.Name}}, error)
	GetAll{{.Name}}s(skip int64, limit int64, selector interface{}) ([]models.{{.Name}}, error)
	GetAll{{.Name}}sBy(search string, selector interface{}, skip int64, limit int64) ([]models.{{.Name}}, error)
    // Write any additional methods and implement them in database package
}

{{end}}